package main

import (
	"fmt"
	"strings"
)

func main()  {
	fmt.Println(Pasword())
}

func Pasword(){
	var b []int
	var s int
	for i := 0; i < 1; i++ {
		fmt.Scan(&s)
		b = append(b, s)
		reslust := strings.Split(s,"")
		
	}
}

